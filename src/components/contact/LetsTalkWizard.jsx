import React from'react';
import TelegramCTA from'./TelegramCTA';

const LetsTalkWizard = () => {
  return (
    <div className="animate-in fade-in zoom-in duration-500">
      <TelegramCTA />
    </div>
  );
};

export default LetsTalkWizard;
